function toggleDay(day){let i=state.done.indexOf(day);if(i>=0){state.done.splice(i,1);selected=day;toast(`Day ${day} reopened`)}else{if(day>nextDay())return;state.done.push(day);state.done.sort((a,b)=>a-b);touch();selected=Math.min(90,nextDay());toast(`Day ${day} complete`)}save();days();lesson(day);dash();}
function stats(){return{day:nextDay(),days:state.done.length,v:Object.values(state.vstat).filter(x=>x==='Learned').length,k:state.klearn.length,g:state.gdone.length,kana:state.done.filter(x=>x<=20).length,life:state.done.filter(x=>x>=41&&x<=60).length,all:P(state.done.length,90)}}
function render(p){({dashboard:dash,course:course,kana:kanaPage,vocabulary:vocabPage,kanji:kanjiPage,grammar:grammarPage,speaking:speakPage,conversations:convPage,life:lifePage,culture:culturePage,reading:readingPage,practice:practicePage,review:reviewPage,progress:progressPage})[p]()}
